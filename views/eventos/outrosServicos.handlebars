<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Serviços Gerais</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #007BFF;
            --highlight: #FFA500;
            --secondary: #FFFFFF;
            --text-dark: #2c3e50;
            --text-light: #6c757d;
            --shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s ease;
        }

        body {
            background-color: #f8f9fa;
            font-family: 'Poppins', sans-serif;
        }

        .page-title {
            color: var(--text-dark);
            font-weight: 700;
            margin-top: 2rem;
            margin-bottom: 1.5rem;
            padding-bottom: 0.5rem;
            border-bottom: 3px solid var(--highlight);
            font-size: 2.2rem;
        }

        .search-bar {
            margin-bottom: 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 20px;
        }

        .search-input {
            width: 70%;
            padding: 12px 20px;
            border: 2px solid #e9ecef;
            border-radius: 12px;
            font-size: 1rem;
            transition: var(--transition);
            box-shadow: var(--shadow);
        }

        .search-input:focus {
            border-color: var(--primary);
            outline: none;
            box-shadow: 0 0 0 3px rgba(0, 123, 255, 0.1);
        }

        .highlight-button {
            background-color: var(--highlight);
            color: white;
            padding: 12px 25px;
            border-radius: 12px;
            text-decoration: none;
            font-weight: 600;
            transition: var(--transition);
            border: none;
            box-shadow: var(--shadow);
        }

        .highlight-button:hover {
            background-color: #e69500;
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
        }

        .filter-sidebar {
            background-color: white;
            padding: 25px;
            border-radius: 15px;
            box-shadow: var(--shadow);
            position: sticky;
            top: 20px;
            z-index: 10;
        }

        .filter-title {
            font-weight: 600;
            margin-bottom: 20px;
            color: var(--text-dark);
            font-size: 1.2rem;
            padding-bottom: 10px;
            border-bottom: 2px solid var(--highlight);
        }

        .category-link {
            display: block;
            padding: 12px 15px;
            color: var(--text-dark);
            text-decoration: none;
            border-radius: 8px;
            transition: var(--transition);
            margin-bottom: 8px;
            font-weight: 500;
            background-color: #f8f9fa;
        }

        .category-link:hover {
            background-color: var(--primary);
            color: white;
            transform: translateX(5px);
        }

        .category-link.active {
            background-color: var(--primary);
            color: white;
            font-weight: 600;
        }

        .card {
            border: none;
            box-shadow: var(--shadow);
            transition: var(--transition);
            border-radius: 15px;
            overflow: hidden;
            height: 100%;
            background-color: white;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
        }

        .card img {
            height: 200px;
            object-fit: cover;
            transition: var(--transition);
        }

        .card:hover img {
            transform: scale(1.05);
        }

        .card-body {
            padding: 25px;
        }

        .card-title {
            color: var(--text-dark);
            font-weight: 600;
            font-size: 1.3rem;
            margin-bottom: 15px;
        }

        .card-text.description {
            color: var(--text-light);
            font-size: 0.95rem;
            line-height: 1.6;
            margin-bottom: 15px;
        }

        .btn-primary {
            background-color: var(--highlight);
            border: none;
            padding: 10px 20px;
            border-radius: 8px;
            font-weight: 500;
            transition: var(--transition);
        }

        .btn-whatsapp {
            background-color: #25D366;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 8px;
            font-weight: 500;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 8px;
            margin-top: auto;
            width: 100%;
            margin-bottom: 1rem;
        }

        .btn-whatsapp:hover {
            background-color: #128C7E;
            color: white;
            transform: translateY(-2px);
        }

        .btn-primary:hover {
            background-color: #e69500;
            transform: translateY(-2px);
        }

        .premium-badge {
            position: absolute;
            top: 15px;
            right: 15px;
            background-color: #FFD700;
            color: #000;
            padding: 8px 15px;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
            box-shadow: var(--shadow);
        }

        .top-week-badge {
            position: absolute;
            top: 18px;
            left: 18px;
            background: linear-gradient(90deg, #ffb300 0%, #ffd700 100%);
            color: #222;
            padding: 8px 22px 8px 18px;
            border-radius: 20px 8px 20px 8px;
            font-size: 1rem;
            font-weight: 700;
            box-shadow: 0 2px 8px rgba(255, 183, 0, 0.18);
            letter-spacing: 1px;
            display: flex;
            align-items: center;
            z-index: 2;
            border: 2px solid #fffbe6;
            text-shadow: 0 1px 2px #fffbe6;
        }

        .top-week-badge i {
            margin-right: 8px;
            color: #ff9800;
        }

        .no-services {
            text-align: center;
            padding: 3rem;
            background-color: white;
            border-radius: 15px;
            box-shadow: var(--shadow);
        }

        .no-services h3 {
            color: var(--text-dark);
            font-weight: 600;
            margin-bottom: 1rem;
        }

        .no-services p {
            color: var(--text-light);
        }

        .post-button {
            position: fixed;
            bottom: 30px;
            right: 30px;
            background-color: var(--highlight);
            color: white;
            padding: 15px 30px;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 600;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            z-index: 1000;
            transition: var(--transition);
        }

        .post-button:hover {
            background-color: #e69500;
            color: white;
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.25);
        }

        @media (max-width: 768px) {
            .container {
                padding: 1rem;
            }

            .search-bar {
                flex-direction: column;
                gap: 15px;
            }

            .search-input {
                width: 100%;
            }

            .highlight-button {
                width: 100%;
                text-align: center;
            }

            .page-title {
                font-size: 1.8rem;
            }

            .col-md-3 {
                margin-bottom: 20px;
            }

            .filter-sidebar {
                position: static;
            }

            .col-md-9 h2 {
                margin-top: 1.5rem;
            }

            .card-body {
                padding: 20px;
            }

            .card-title {
                font-size: 1.2rem;
            }

            .card img {
                height: 180px;
            }

            .premium-badge, .top-week-badge {
                padding: 6px 12px;
                font-size: 0.75rem;
            }

            .no-services {
                padding: 2rem;
            }

            .post-button {
                bottom: 20px;
                right: 20px;
                padding: 12px 25px;
                font-size: 0.9rem;
            }
        }

        .category-carousel {
            display: flex;
            flex-direction: row;
            flex-wrap: nowrap;
            overflow-x: auto;
            gap: 1rem;
            padding-bottom: 1rem;
            scroll-behavior: smooth;
        }
        .category-carousel .service-card {
            min-width: 320px;
            max-width: 320px;
            flex: 0 0 320px;
            display: flex;
            height: 100%;
        }
        .card.h-100 {
            display: flex;
            flex-direction: column;
            height: 100%;
            min-height: 420px;
            position: relative;
            background: #fff;
        }
        .card-img-top {
            height: 180px;
            object-fit: cover;
            border-top-left-radius: 15px;
            border-top-right-radius: 15px;
            width: 100%;
        }
        .card-body {
            flex: 1 1 auto;
            display: flex;
            flex-direction: column;
            justify-content: flex-start;
            padding-bottom: 0;
        }
        .card-title {
            margin-bottom: 0.5rem;
            min-height: 2.2em;
        }
        .card-text.description {
            min-height: 2.8em;
            margin-bottom: 0.5rem;
        }
        .btn-whatsapp {
            margin-top: auto;
            width: 100%;
        }
        .top-week-badge {
            position: absolute;
            top: 18px;
            left: 18px;
            background: linear-gradient(90deg, #ffb300 0%, #ffd700 100%);
            color: #222;
            padding: 8px 22px 8px 18px;
            border-radius: 20px 8px 20px 8px;
            font-size: 1rem;
            font-weight: 700;
            box-shadow: 0 2px 8px rgba(255, 183, 0, 0.18);
            letter-spacing: 1px;
            display: flex;
            align-items: center;
            z-index: 2;
            border: 2px solid #fffbe6;
            text-shadow: 0 1px 2px #fffbe6;
        }
        .top-week-badge i {
            margin-right: 8px;
            color: #ff9800;
        }
        .category-title {
            font-size: 1.4rem;
            font-weight: 600;
            color: var(--text-dark);
            margin-top: 2.5rem;
            margin-bottom: 0.5rem;
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <h1 class="page-title">Serviços Gerais</h1>

        <div class="search-bar">
            <input type="text" class="search-input" id="searchInput" placeholder="Busque por um serviço...">
            {{#if session.userid}}
                {{#if isPremium}}
                    <a href="/premium/post-form" class="highlight-button">Divulgar trabalho</a>
                {{else}}
                    <a href="/premium" class="highlight-button">Seja Premium para divulgar</a>
                {{/if}}
            {{else}}
                <a href="/login" class="highlight-button">Faça login para divulgar</a>
            {{/if}}
        </div>

        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-3">
                <div class="filter-sidebar">
                    <div class="filter-title">Categorias</div>
                    <div class="categories-list">
                        <a href="#top" class="category-link">Destaques</a>
                        <a href="#construcao" class="category-link">Construção</a>
                        <a href="#eletrica" class="category-link">Elétrica</a>
                        <a href="#hidraulica" class="category-link">Hidráulica</a>
                        <a href="#jardinagem" class="category-link">Jardinagem</a>
                        <a href="#limpeza" class="category-link">Limpeza</a>
                        <a href="#manutencao" class="category-link">Manutenção</a>
                        <a href="#outros" class="category-link">Outros</a>
                    </div>
                </div>
            </div>

            <!-- Main Content -->
            <div class="col-md-9">
                <h2 class="mb-4">
                    {{#if selectedCategory}}
                        Serviços de {{selectedCategory}}
                    {{else}}
                        Todos os Serviços
                    {{/if}}
                </h2>
                {{#if premiumPosts}}
                    <h2 class="category-title" id="top">Serviços em Destaque</h2>
                    <hr>
                    <div class="category-carousel">
                        {{#each premiumPosts}}
                            {{#if this.isTopWeek}}
                                <div class="col-12 col-md-4 me-3 flex-shrink-0 service-card">
                                    <div class="card h-100">
                                        {{#if this.coverImage}}
                                            <img src="{{this.coverImage}}" class="card-img-top" alt="{{this.title}}">
                                        {{else}}
                                            <img src="/img/default-service.jpg" class="card-img-top" alt="Imagem padrão">
                                        {{/if}}
                                        <div class="top-week-badge"><i class="fas fa-star"></i>Serviço Top</div>
                                        <div class="card-body">
                                            <h5 class="card-title">{{this.title}}</h5>
                                            {{#if this.description}}
                                                <p class="card-text description">{{this.description}}</p>
                                            {{/if}}
                                            <p class="card-text">Tipo: {{this.workType}}</p>
                                            <a href="#" class="btn btn-whatsapp contact-btn" data-whatsapp="https://wa.me/{{formatPhone this.whatsappLink}}">
                                                <i class="fab fa-whatsapp"></i> Contatar
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            {{/if}}
                        {{/each}}
                    </div>
                {{/if}}
                <h3 class="category-title" id="construcao">Construção</h3>
                <hr>
                <div class="category-carousel">
                    {{#each premiumPosts}}
                        {{#if (eq this.workType "construcao")}}
                            <div class="col-12 col-md-4 me-3 flex-shrink-0 service-card">
                                <div class="card h-100">
                                    {{#if this.coverImage}}
                                        <img src="{{this.coverImage}}" class="card-img-top" alt="{{this.title}}">
                                    {{else}}
                                        <img src="/img/default-service.jpg" class="card-img-top" alt="Imagem padrão">
                                    {{/if}}
                                    {{#if this.isTopWeek}}
                                        <div class="top-week-badge"><i class="fas fa-star"></i>Serviço Top</div>
                                    {{/if}}
                                    <div class="card-body">
                                        <h5 class="card-title">{{this.title}}</h5>
                                        {{#if this.description}}
                                            <p class="card-text description">{{this.description}}</p>
                                        {{/if}}
                                        <p class="card-text">Tipo: {{this.workType}}</p>
                                        <a href="#" class="btn btn-whatsapp contact-btn" data-whatsapp="https://wa.me/{{formatPhone this.whatsappLink}}">
                                            <i class="fab fa-whatsapp"></i> Contatar
                                        </a>
                                    </div>
                                </div>
                            </div>
                        {{/if}}
                    {{/each}}
                </div>

                <h3 class="category-title" id="eletrica">Elétrica</h3>
                <hr>
                <div class="category-carousel">
                    {{#each premiumPosts}}
                        {{#if (eq this.workType "eletrica")}}
                            <div class="col-12 col-md-4 me-3 flex-shrink-0 service-card">
                                <div class="card h-100">
                                    {{#if this.coverImage}}
                                        <img src="{{this.coverImage}}" class="card-img-top" alt="{{this.title}}">
                                    {{else}}
                                        <img src="/img/default-service.jpg" class="card-img-top" alt="Imagem padrão">
                                    {{/if}}
                                    {{#if this.isTopWeek}}
                                        <div class="top-week-badge"><i class="fas fa-star"></i>Serviço Top</div>
                                    {{/if}}
                                    <div class="card-body">
                                        <h5 class="card-title">{{this.title}}</h5>
                                        {{#if this.description}}
                                            <p class="card-text description">{{this.description}}</p>
                                        {{/if}}
                                        <p class="card-text">Tipo: {{this.workType}}</p>
                                        <a href="#" class="btn btn-whatsapp contact-btn" data-whatsapp="https://wa.me/{{formatPhone this.whatsappLink}}">
                                            <i class="fab fa-whatsapp"></i> Contatar
                                        </a>
                                    </div>
                                </div>
                            </div>
                        {{/if}}
                    {{/each}}
                </div>

                <h3 class="category-title" id="hidraulica">Hidráulica</h3>
                <hr>
                <div class="category-carousel">
                    {{#each premiumPosts}}
                        {{#if (eq this.workType "hidraulica")}}
                            <div class="col-12 col-md-4 me-3 flex-shrink-0 service-card">
                                <div class="card h-100">
                                    {{#if this.coverImage}}
                                        <img src="{{this.coverImage}}" class="card-img-top" alt="{{this.title}}">
                                    {{else}}
                                        <img src="/img/default-service.jpg" class="card-img-top" alt="Imagem padrão">
                                    {{/if}}
                                    {{#if this.isTopWeek}}
                                        <div class="top-week-badge"><i class="fas fa-star"></i>Serviço Top</div>
                                    {{/if}}
                                    <div class="card-body">
                                        <h5 class="card-title">{{this.title}}</h5>
                                        {{#if this.description}}
                                            <p class="card-text description">{{this.description}}</p>
                                        {{/if}}
                                        <p class="card-text">Tipo: {{this.workType}}</p>
                                        <a href="#" class="btn btn-whatsapp contact-btn" data-whatsapp="https://wa.me/{{formatPhone this.whatsappLink}}">
                                            <i class="fab fa-whatsapp"></i> Contatar
                                        </a>
                                    </div>
                                </div>
                            </div>
                        {{/if}}
                    {{/each}}
                </div>

                <h3 class="category-title" id="jardinagem">Jardinagem</h3>
                <hr>
                <div class="category-carousel">
                    {{#each premiumPosts}}
                        {{#if (eq this.workType "jardinagem")}}
                            <div class="col-12 col-md-4 me-3 flex-shrink-0 service-card">
                                <div class="card h-100">
                                    {{#if this.coverImage}}
                                        <img src="{{this.coverImage}}" class="card-img-top" alt="{{this.title}}">
                                    {{else}}
                                        <img src="/img/default-service.jpg" class="card-img-top" alt="Imagem padrão">
                                    {{/if}}
                                    {{#if this.isTopWeek}}
                                        <div class="top-week-badge"><i class="fas fa-star"></i>Serviço Top</div>
                                    {{/if}}
                                    <div class="card-body">
                                        <h5 class="card-title">{{this.title}}</h5>
                                        {{#if this.description}}
                                            <p class="card-text description">{{this.description}}</p>
                                        {{/if}}
                                        <p class="card-text">Tipo: {{this.workType}}</p>
                                        <a href="#" class="btn btn-whatsapp contact-btn" data-whatsapp="https://wa.me/{{formatPhone this.whatsappLink}}">
                                            <i class="fab fa-whatsapp"></i> Contatar
                                        </a>
                                    </div>
                                </div>
                            </div>
                        {{/if}}
                    {{/each}}
                </div>

                <h3 class="category-title" id="limpeza">Limpeza</h3>
                <hr>
                <div class="category-carousel">
                    {{#each premiumPosts}}
                        {{#if (eq this.workType "limpeza")}}
                            <div class="col-12 col-md-4 me-3 flex-shrink-0 service-card">
                                <div class="card h-100">
                                    {{#if this.coverImage}}
                                        <img src="{{this.coverImage}}" class="card-img-top" alt="{{this.title}}">
                                    {{else}}
                                        <img src="/img/default-service.jpg" class="card-img-top" alt="Imagem padrão">
                                    {{/if}}
                                    {{#if this.isTopWeek}}
                                        <div class="top-week-badge"><i class="fas fa-star"></i>Serviço Top</div>
                                    {{/if}}
                                    <div class="card-body">
                                        <h5 class="card-title">{{this.title}}</h5>
                                        {{#if this.description}}
                                            <p class="card-text description">{{this.description}}</p>
                                        {{/if}}
                                        <p class="card-text">Tipo: {{this.workType}}</p>
                                        <a href="#" class="btn btn-whatsapp contact-btn" data-whatsapp="https://wa.me/{{formatPhone this.whatsappLink}}">
                                            <i class="fab fa-whatsapp"></i> Contatar
                                        </a>
                                    </div>
                                </div>
                            </div>
                        {{/if}}
                    {{/each}}
                </div>

                <h3 class="category-title" id="manutencao">Manutenção</h3>
                <hr>
                <div class="category-carousel">
                    {{#each premiumPosts}}
                        {{#if (eq this.workType "manutencao")}}
                            <div class="col-12 col-md-4 me-3 flex-shrink-0 service-card">
                                <div class="card h-100">
                                    {{#if this.coverImage}}
                                        <img src="{{this.coverImage}}" class="card-img-top" alt="{{this.title}}">
                                    {{else}}
                                        <img src="/img/default-service.jpg" class="card-img-top" alt="Imagem padrão">
                                    {{/if}}
                                    {{#if this.isTopWeek}}
                                        <div class="top-week-badge"><i class="fas fa-star"></i>Serviço Top</div>
                                    {{/if}}
                                    <div class="card-body">
                                        <h5 class="card-title">{{this.title}}</h5>
                                        {{#if this.description}}
                                            <p class="card-text description">{{this.description}}</p>
                                        {{/if}}
                                        <p class="card-text">Tipo: {{this.workType}}</p>
                                        <a href="#" class="btn btn-whatsapp contact-btn" data-whatsapp="https://wa.me/{{formatPhone this.whatsappLink}}">
                                            <i class="fab fa-whatsapp"></i> Contatar
                                        </a>
                                    </div>
                                </div>
                            </div>
                        {{/if}}
                    {{/each}}
                </div>

                <h3 class="category-title" id="outros">Outros</h3>
                <hr>
                <div class="category-carousel">
                    {{#each premiumPosts}}
                        {{#if (eq this.workType "outros")}}
                            <div class="col-12 col-md-4 me-3 flex-shrink-0 service-card">
                                <div class="card h-100">
                                    {{#if this.coverImage}}
                                        <img src="{{this.coverImage}}" class="card-img-top" alt="{{this.title}}">
                                    {{else}}
                                        <img src="/img/default-service.jpg" class="card-img-top" alt="Imagem padrão">
                                    {{/if}}
                                    {{#if this.isTopWeek}}
                                        <div class="top-week-badge"><i class="fas fa-star"></i>Serviço Top</div>
                                    {{/if}}
                                    <div class="card-body">
                                        <h5 class="card-title">{{this.title}}</h5>
                                        {{#if this.description}}
                                            <p class="card-text description">{{this.description}}</p>
                                        {{/if}}
                                        <p class="card-text">Tipo: {{this.workType}}</p>
                                        <a href="#" class="btn btn-whatsapp contact-btn" data-whatsapp="https://wa.me/{{formatPhone this.whatsappLink}}">
                                            <i class="fab fa-whatsapp"></i> Contatar
                                        </a>
                                    </div>
                                </div>
                            </div>
                        {{/if}}
                    {{/each}}
                </div>
            </div>
        </div>
    </div>

    {{#if session.userid}}
        {{#if isPremium}}
            <a href="/premium/post-form" class="post-button">
                <i class="fas fa-plus"></i> Divulgar Trabalho
            </a>
        {{/if}}
    {{/if}}

    <!-- Modal de Alerta -->
    <div class="modal fade" id="alertModal" tabindex="-1" aria-labelledby="alertModalLabel" aria-hidden="true">
      <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
          <div class="modal-header bg-warning">
            <h5 class="modal-title" id="alertModalLabel">Atenção!</h5>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fechar"></button>
          </div>
          <div class="modal-body">
            <p><strong>Nunca aceite pagar valores antecipados!</strong></p>
            <p>Desconfie de qualquer pedido de pagamento antes da prestação do serviço. Não envie dinheiro, pix ou depósitos antecipados para desconhecidos.</p>
            <ul>
              <li>Negocie pessoalmente sempre que possível.</li>
              <li>Desconfie de ofertas muito vantajosas.</li>
              <li>Denuncie qualquer tentativa de golpe.</li>
            </ul>
            <p>Este site não se responsabiliza por negociações.</p>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Entendi</button>
            <a id="continueWhatsapp" href="#" target="_blank" class="btn btn-success">
              Ir para o WhatsApp
            </a>
          </div>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Funcionalidade de pesquisa
        document.getElementById('searchInput').addEventListener('keyup', function() {
            const searchTerm = this.value.toLowerCase();
            const cards = document.querySelectorAll('.service-card');
            
            cards.forEach(card => {
                const title = card.querySelector('.card-title').textContent.toLowerCase();
                const description = card.querySelector('.card-text.description')?.textContent.toLowerCase() || '';
                const workType = card.querySelector('.card-text:last-of-type').textContent.toLowerCase();
                
                if (title.includes(searchTerm) || description.includes(searchTerm) || workType.includes(searchTerm)) {
                    card.style.display = '';
                } else {
                    card.style.display = 'none';
                }
            });
        });

        // Alerta de segurança ao clicar em "Contatar"
        document.querySelectorAll('.contact-btn').forEach(btn => {
            btn.addEventListener('click', function(e) {
                e.preventDefault();
                const whatsappLink = this.getAttribute('data-whatsapp');
                const continueBtn = document.getElementById('continueWhatsapp');
                continueBtn.setAttribute('href', whatsappLink);
                const alertModal = new bootstrap.Modal(document.getElementById('alertModal'));
                alertModal.show();
            });
        });
    </script>
</body>
</html>