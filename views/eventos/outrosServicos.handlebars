<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Serviços Gerais</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #007BFF;
            --highlight: #FFA500;
            --secondary: #FFFFFF;
            --text-dark: #2c3e50;
            --text-light: #6c757d;
            --shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s ease;
        }

        body {
            background-color: #f8f9fa;
            font-family: 'Poppins', sans-serif;
        }

        .page-title {
            color: var(--text-dark);
            font-weight: 700;
            margin-top: 2rem;
            margin-bottom: 1.5rem;
            padding-bottom: 0.5rem;
            border-bottom: 3px solid var(--highlight);
            font-size: 2.2rem;
        }

        .search-bar {
            margin-bottom: 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 20px;
        }

        .search-input {
            width: 70%;
            padding: 12px 20px;
            border: 2px solid #e9ecef;
            border-radius: 12px;
            font-size: 1rem;
            transition: var(--transition);
            box-shadow: var(--shadow);
        }

        .search-input:focus {
            border-color: var(--primary);
            outline: none;
            box-shadow: 0 0 0 3px rgba(0, 123, 255, 0.1);
        }

        .highlight-button {
            background-color: var(--highlight);
            color: white;
            padding: 12px 25px;
            border-radius: 12px;
            text-decoration: none;
            font-weight: 600;
            transition: var(--transition);
            border: none;
            box-shadow: var(--shadow);
        }

        .highlight-button:hover {
            background-color: #e69500;
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
        }

        .filter-sidebar {
            background-color: white;
            padding: 25px;
            border-radius: 15px;
            box-shadow: var(--shadow);
            position: sticky;
            top: 20px;
        }

        .filter-title {
            font-weight: 600;
            margin-bottom: 20px;
            color: var(--text-dark);
            font-size: 1.2rem;
            padding-bottom: 10px;
            border-bottom: 2px solid var(--highlight);
        }

        .category-link {
            display: block;
            padding: 12px 15px;
            color: var(--text-dark);
            text-decoration: none;
            border-radius: 8px;
            transition: var(--transition);
            margin-bottom: 8px;
            font-weight: 500;
            background-color: #f8f9fa;
        }

        .category-link:hover {
            background-color: var(--primary);
            color: white;
            transform: translateX(5px);
        }

        .category-link.active {
            background-color: var(--primary);
            color: white;
            font-weight: 600;
        }

        .card {
            border: none;
            box-shadow: var(--shadow);
            transition: var(--transition);
            border-radius: 15px;
            overflow: hidden;
            height: 100%;
            background-color: white;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
        }

        .card img {
            height: 200px;
            object-fit: cover;
            transition: var(--transition);
        }

        .card:hover img {
            transform: scale(1.05);
        }

        .card-body {
            padding: 25px;
        }

        .card-title {
            color: var(--text-dark);
            font-weight: 600;
            font-size: 1.3rem;
            margin-bottom: 15px;
        }

        .card-text.description {
            color: var(--text-light);
            font-size: 0.95rem;
            line-height: 1.6;
            margin-bottom: 15px;
        }

        .btn-primary {
            background-color: var(--highlight);
            border: none;
            padding: 10px 20px;
            border-radius: 8px;
            font-weight: 500;
            transition: var(--transition);
        }

        .btn-whatsapp {
            background-color: #25D366;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 8px;
            font-weight: 500;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .btn-whatsapp:hover {
            background-color: #128C7E;
            color: white;
            transform: translateY(-2px);
        }

        .btn-primary:hover {
            background-color: #e69500;
            transform: translateY(-2px);
        }

        .premium-badge {
            position: absolute;
            top: 15px;
            right: 15px;
            background-color: #FFD700;
            color: #000;
            padding: 8px 15px;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
            box-shadow: var(--shadow);
        }

        .top-week-badge {
            position: absolute;
            top: 15px;
            left: 15px;
            background-color: #28a745;
            color: white;
            padding: 8px 15px;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
            box-shadow: var(--shadow);
        }

        .no-services {
            text-align: center;
            padding: 3rem;
            background-color: white;
            border-radius: 15px;
            box-shadow: var(--shadow);
        }

        .no-services h3 {
            color: var(--text-dark);
            font-weight: 600;
            margin-bottom: 1rem;
        }

        .no-services p {
            color: var(--text-light);
        }

        .post-button {
            position: fixed;
            bottom: 30px;
            right: 30px;
            background-color: var(--highlight);
            color: white;
            padding: 15px 30px;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 600;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            z-index: 1000;
            transition: var(--transition);
        }

        .post-button:hover {
            background-color: #e69500;
            color: white;
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.25);
        }

        @media (max-width: 768px) {
            .container {
                padding: 1rem;
            }

            .search-bar {
                flex-direction: column;
                gap: 15px;
            }

            .search-input {
                width: 100%;
            }

            .highlight-button {
                width: 100%;
                text-align: center;
            }

            .page-title {
                font-size: 1.8rem;
            }

            .col-md-3 {
                margin-bottom: 20px;
            }

            .filter-sidebar {
                position: static;
            }

            .col-md-9 h2 {
                margin-top: 1.5rem;
            }

            .card-body {
                padding: 20px;
            }

            .card-title {
                font-size: 1.2rem;
            }

            .card img {
                height: 180px;
            }

            .premium-badge, .top-week-badge {
                padding: 6px 12px;
                font-size: 0.75rem;
            }

            .no-services {
                padding: 2rem;
            }

            .post-button {
                bottom: 20px;
                right: 20px;
                padding: 12px 25px;
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <h1 class="page-title">Serviços Gerais</h1>

        <div class="search-bar">
            <input type="text" class="search-input" id="searchInput" placeholder="Busque por um serviço...">
            {{#if session.userid}}
                {{#if isPremium}}
                    <a href="/premium/post-form" class="highlight-button">Divulgar trabalho</a>
                {{else}}
                    <a href="/premium" class="highlight-button">Seja Premium para divulgar</a>
                {{/if}}
            {{else}}
                <a href="/login" class="highlight-button">Faça login para divulgar</a>
            {{/if}}
        </div>

        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-3">
                <div class="filter-sidebar">
                    <div class="filter-title">Categorias</div>
                    {{#if workTypes}}
                        <div class="categories-list">
                            {{#each workTypes}}
                                <a href="?category={{this}}" class="category-link{{#if ../selectedCategory}}{{#if (eq ../selectedCategory this)}} active{{/if}}{{/if}}">{{this}}</a>
                            {{/each}}
                        </div>
                    {{else}}
                        <p class="text-muted">Nenhuma categoria disponível</p>
                    {{/if}}
                </div>
            </div>

            <!-- Main Content -->
            <div class="col-md-9">
                <h2 class="mb-4">
                    {{#if selectedCategory}}
                        Serviços de {{selectedCategory}}
                    {{else}}
                        Todos os Serviços
                    {{/if}}
                </h2>
                {{#if premiumPosts}}
                    <div class="row row-cols-1 row-cols-md-3 g-4">
                        {{#each premiumPosts}}
                            <div class="col service-card">
                                <div class="card">
                                    {{#if this.coverImage}}
                                        <img src="{{this.coverImage}}" class="card-img-top" alt="{{this.title}}">
                                    {{else}}
                                        <img src="/img/default-service.jpg" class="card-img-top" alt="Imagem padrão">
                                    {{/if}}
                                    {{#if this.isTopWeek}}
                                        <div class="top-week-badge">Serviço Top</div>
                                    {{/if}}
                                    <div class="card-body">
                                        <h5 class="card-title">{{this.title}}</h5>
                                        {{#if this.description}}
                                            <p class="card-text description">{{this.description}}</p>
                                        {{/if}}
                                        <p class="card-text">Tipo: {{this.workType}}</p>
                                        <a href="https://wa.me/{{formatPhone this.whatsappLink}}" class="btn btn-whatsapp" target="_blank">
                                            <i class="fab fa-whatsapp"></i> Contatar
                                        </a>
                                    </div>
                                </div>
                            </div>
                        {{/each}}
                    </div>
                {{else}}
                    <div class="no-services">
                        <h3>Nenhum serviço disponível no momento</h3>
                        <p>Seja o primeiro a divulgar seu trabalho!</p>
                    </div>
                {{/if}}
            </div>
        </div>
    </div>

    {{#if session.userid}}
        {{#if isPremium}}
            <a href="/premium/post-form" class="post-button">
                <i class="fas fa-plus"></i> Divulgar Trabalho
            </a>
        {{/if}}
    {{/if}}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Funcionalidade de pesquisa
        document.getElementById('searchInput').addEventListener('keyup', function() {
            const searchTerm = this.value.toLowerCase();
            const cards = document.querySelectorAll('.service-card');
            
            cards.forEach(card => {
                const title = card.querySelector('.card-title').textContent.toLowerCase();
                const description = card.querySelector('.card-text.description')?.textContent.toLowerCase() || '';
                const workType = card.querySelector('.card-text:last-of-type').textContent.toLowerCase();
                
                if (title.includes(searchTerm) || description.includes(searchTerm) || workType.includes(searchTerm)) {
                    card.style.display = '';
                } else {
                    card.style.display = 'none';
                }
            });
        });
    </script>
</body>
</html>