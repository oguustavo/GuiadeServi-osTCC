<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vagas de Emprego</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary: #007BFF;
            --secondary: #FFFFFF;
            --support: #03A9F4;
            --highlight: #FFA500;
        }
        body {
            background-color: #f8f9fa;
        }
        .page-title {
            color: var(--primary);
            font-weight: 600;
            margin-bottom: 1.5rem;
            padding-bottom: 0.5rem;
            border-bottom: 3px solid var(--highlight);
        }

        .vaga-card {
            border: none;
            box-shadow: 0 2px 15px rgba(0,0,0,0.1);
            margin-bottom: 20px;
            transition: transform 0.2s;
        }

        .vaga-card:hover {
            transform: translateY(-5px);
        }

        .vaga-header {
            background-color: var(--primary);
            color: white;
            padding: 15px;
            border-radius: 10px 10px 0 0;
        }

        .vaga-tipo {
            background-color: var(--highlight);
            color: white;
            padding: 3px 10px;
            border-radius: 15px;
            font-size: 0.9rem;
            display: inline-block;
        }

        .vaga-empresa {
            color: var(--primary);
            font-weight: 500;
        }

        .vaga-requisitos {
            background-color: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
            margin: 10px 0;
        }

        .vaga-contato {
            background-color: #e9ecef;
            padding: 15px;
            border-radius: 0 0 10px 10px;
        }

        .sem-vagas {
            text-align: center;
            padding: 50px;
            background-color: #f8f9fa;
            border-radius: 10px;
            margin: 20px 0;
            box-shadow: 0 2px 15px rgba(0,0,0,0.1);
        }

        .sem-vagas i {
            font-size: 3rem;
            color: var(--primary);
            margin-bottom: 20px;
        }

        .card-title {
            margin: 0;
            font-size: 1.25rem;
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <h1 class="page-title">Vagas de emprego</h1>
        
        {{#if vagas.length}}
            <div class="row">
                {{#each vagas}}
                    <div class="col-12 col-md-6 col-lg-4">
                        <div class="vaga-card card">
                            <div class="vaga-header">
                                <h3 class="card-title mb-2">{{cargo}}</h3>
                                <span class="vaga-tipo">{{tipoVaga}}</span>
                            </div>
                            <div class="card-body">
                                <h4 class="vaga-empresa mb-3">
                                    <i class="fas fa-building me-2"></i>{{empresa}}
                                </h4>
                                
                                <div class="d-flex align-items-center mb-3">
                                    <i class="fas fa-location-dot me-2 text-primary"></i>
                                    <span>{{endereco}}</span>
                                </div>
                                
                                <div class="vaga-requisitos">
                                    <h5 class="mb-2"><i class="fas fa-list-check me-2"></i>Requisitos:</h5>
                                    <p class="mb-0">{{requisitos}}</p>
                                </div>
                                
                                <div class="vaga-contato mt-3">
                                    <h5 class="mb-2"><i class="fas fa-envelope me-2"></i>Contato:</h5>
                                    <p class="mb-0">{{contato}}</p>
                                </div>
                            </div>
                        </div>
                    </div>
                {{/each}}
            </div>
        {{else}}
            <div class="sem-vagas">
                <i class="fas fa-briefcase mb-3"></i>
                <h3>Nenhuma vaga disponível no momento</h3>
                <p class="text-muted">Fique de olho! Novas oportunidades serão publicadas em breve.</p>
            </div>
        {{/if}}
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 