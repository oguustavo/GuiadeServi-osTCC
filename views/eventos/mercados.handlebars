<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mercados e Restaurantes Abertos</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary: #007BFF;
            --secondary: #FFFFFF;
            --support: #03A9F4;
            --highlight: #FFA500;
        }
        body {
            background-color: #f8f9fa;
        }
        .page-title {
            color: var(--primary);
            font-weight: 600;
            margin-bottom: 1.5rem;
            padding-bottom: 0.5rem;
            border-bottom: 3px solid var(--highlight);
        }
        .section-title {
            color: var(--primary);
            font-weight: 600;
            margin-bottom: 1rem;
            padding-bottom: 0.5rem;
            border-bottom: 2px solid var(--highlight);
        }
        .section-status {
            color: #28a745;
            font-size: 0.9em;
            margin-left: 1rem;
        }
        .card {
            transition: transform 0.2s;
            margin-bottom: 20px;
            height: 100%;
        }
        .card:hover {
            transform: scale(1.02);
        }
        .card-img-top {
            height: 200px;
            object-fit: cover;
        }
        .estabelecimento-tipo {
            position: absolute;
            top: 10px;
            right: 10px;
            background-color: var(--highlight);
            color: white;
            padding: 5px 10px;
            border-radius: 15px;
            font-size: 0.9em;
        }
        .card-footer {
            background-color: transparent;
            border-top: none;
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <h1 class="page-title">Estabelecimentos Abertos</h1>
        
        {{#if estabelecimentos}}
            <!-- Seção de Lojas -->
            <h2 class="section-title">LOJAS<span class="section-status">Lojas abertas</span></h2>
            <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4 mb-5">
                {{#each estabelecimentos}}
                    {{#if (isEqual this.subtipo "loja")}}
                        <div class="col">
                            <div class="card h-100">
                                {{#if this.imagem}}
                                    <img src="/uploads/{{this.imagem}}" class="card-img-top" alt="{{this.nome}}">
                                {{else}}
                                    <img src="/img/mercado.jpg" class="card-img-top" alt="Imagem padrão">
                                {{/if}}
                                <div class="estabelecimento-tipo">
                                    {{capitalizeFirst this.subtipo}}
                                </div>
                                <div class="card-body">
                                    <h5 class="card-title">{{this.nome}}</h5>
                                    <p class="card-text">
                                        <i class="fas fa-map-marker-alt text-primary me-2"></i>{{this.endereco}}
                                    </p>
                                    <p class="card-text">
                                        <i class="fas fa-phone text-primary me-2"></i>{{this.telefone}}
                                    </p>
                                </div>
                            </div>
                        </div>
                    {{/if}}
                {{/each}}
            </div>

            <!-- Seção de Mercados -->
            <h2 class="section-title">MERCADOS<span class="section-status">Mercados abertos</span></h2>
            <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4 mb-5">
                {{#each estabelecimentos}}
                    {{#if (isEqual this.subtipo "mercado")}}
                        <div class="col">
                            <div class="card h-100">
                                {{#if this.imagem}}
                                    <img src="/uploads/{{this.imagem}}" class="card-img-top" alt="{{this.nome}}">
                                {{else}}
                                    <img src="/img/mercado.jpg" class="card-img-top" alt="Imagem padrão">
                                {{/if}}
                                <div class="estabelecimento-tipo">
                                    {{capitalizeFirst this.subtipo}}
                                </div>
                                <div class="card-body">
                                    <h5 class="card-title">{{this.nome}}</h5>
                                    <p class="card-text">
                                        <i class="fas fa-map-marker-alt text-primary me-2"></i>{{this.endereco}}
                                    </p>
                                    <p class="card-text">
                                        <i class="fas fa-phone text-primary me-2"></i>{{this.telefone}}
                                    </p>
                                </div>
                            </div>
                        </div>
                    {{/if}}
                {{/each}}
            </div>

            <!-- Seção de Restaurantes -->
            <h2 class="section-title">RESTAURANTES<span class="section-status">Restaurantes abertos</span></h2>
            <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
                {{#each estabelecimentos}}
                    {{#if (isEqual this.subtipo "restaurante")}}
                        <div class="col">
                            <div class="card h-100">
                                {{#if this.imagem}}
                                    <img src="/uploads/{{this.imagem}}" class="card-img-top" alt="{{this.nome}}">
                                {{else}}
                                    <img src="/img/mercado.jpg" class="card-img-top" alt="Imagem padrão">
                                {{/if}}
                                <div class="estabelecimento-tipo">
                                    {{capitalizeFirst this.subtipo}}
                                </div>
                                <div class="card-body">
                                    <h5 class="card-title">{{this.nome}}</h5>
                                    <p class="card-text">
                                        <i class="fas fa-map-marker-alt text-primary me-2"></i>{{this.endereco}}
                                    </p>
                                    <p class="card-text">
                                        <i class="fas fa-phone text-primary me-2"></i>{{this.telefone}}
                                    </p>
                                </div>
                            </div>
                        </div>
                    {{/if}}
                {{/each}}
            </div>
        {{else}}
            <div class="alert alert-info" role="alert">
                <i class="fas fa-info-circle me-2"></i>
                Nenhum estabelecimento cadastrado no momento.
            </div>
        {{/if}}
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 